app = "junior"
primary_region = "iad"
swap_size_mb = 512

[build]
  image = "ghcr.io/twohoursonelife/junior:latest"

[[restart]]
  policy = "on-failure"

# Release command, deploy commands

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
